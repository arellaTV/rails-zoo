<h1>Zoo Directory</h1>

<%= link_to 'New zoo', new_zoo_path %>

<div class="zoo-list">
  <% @zoos.each do |zoo| %>
    <div class="zoo-list__item">
      <h2><%= zoo.name %></h2>
      <p><%= zoo.description %></p>
      <p><%= link_to 'Show', zoo_path(zoo) %></p>
      <p><%= link_to 'Edit', edit_zoo_path(zoo) %></td>
    </div>
  <% end %>
</div>